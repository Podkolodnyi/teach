import cs from "./header.module.css";

let Header = () => {
    return (
        <header className={cs.header}>
            <img className={cs.logo} src="" alt = "logo"/>
        </header>
    );
}

export default Header;